module.exports.run = async (bot, message, args) => {
    if(message.content === "!invite 1 1") {
        bot.channels.get("409456956466659330").createInvite({maxAge: 0, maxUses: 1})
        .then(invite => message.channel.send(`1 invitation à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 1 5") {
        bot.channels.get("409456956466659330").createInvite({maxAge: 0, maxUses: 5})
        .then(invite => message.channel.send(`5 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 1 10") {
        bot.channels.get("409456956466659330").createInvite({maxAge: 0, maxUses: 10})
        .then(invite => message.channel.send(`10 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 1 25") {
        bot.channels.get("409456956466659330").createInvite({maxAge: 0, maxUses: 25})
        .then(invite => message.channel.send(`25 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 1 30") {
        bot.channels.get("409456956466659330").createInvite({maxAge: 0, maxUses: 30})
        .then(invite => message.channel.send(`30 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 1 40") {
        bot.channels.get("409456956466659330").createInvite({maxAge: 0, maxUses: 40})
        .then(invite => message.channel.send(`40 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 1 50") {
        bot.channels.get("409456956466659330").createInvite({maxAge: 0, maxUses: 50})
        .then(invite => message.channel.send(`50 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 2 1") {
        bot.channels.get("409456976767090703").createInvite({maxAge: 0, maxUses: 1})
        .then(invite => message.channel.send(`1 invitation à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 2 5") {
        bot.channels.get("409456976767090703").createInvite({maxAge: 0, maxUses: 5})
        .then(invite => message.channel.send(`5 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 2 10") {
        bot.channels.get("409456976767090703").createInvite({maxAge: 0, maxUses: 10})
        .then(invite => message.channel.send(`10 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 2 25") {
        bot.channels.get("409456976767090703").createInvite({maxAge: 0, maxUses: 25})
        .then(invite => message.channel.send(`25 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 2 30") {
        bot.channels.get("409456976767090703").createInvite({maxAge: 0, maxUses: 30})
        .then(invite => message.channel.send(`30 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 3 1") {
        bot.channels.get("409456997679890435").createInvite({maxAge: 0, maxUses: 1})
        .then(invite => message.channel.send(`1 invitation à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 3 5") {
        bot.channels.get("409456997679890435").createInvite({maxAge: 0, maxUses: 5})
        .then(invite => message.channel.send(`5 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 3 10") {
        bot.channels.get("409456997679890435").createInvite({maxAge: 0, maxUses: 10})
        .then(invite => message.channel.send(`10 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 3 25") {
        bot.channels.get("409456997679890435").createInvite({maxAge: 0, maxUses: 25})
        .then(invite => message.channel.send(`25 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 3 30") {
        bot.channels.get("409456997679890435").createInvite({maxAge: 0, maxUses: 30})
        .then(invite => message.channel.send(`30 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 4 1") {
        bot.channels.get("409457023470534666").createInvite({maxAge: 0, maxUses: 1})
        .then(invite => message.channel.send(`1 invitation à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 4 5") {
        bot.channels.get("409457023470534666").createInvite({maxAge: 0, maxUses: 5})
        .then(invite => message.channel.send(`5 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 4 10") {
        bot.channels.get("409457023470534666").createInvite({maxAge: 0, maxUses: 10})
        .then(invite => message.channel.send(`10 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 4 25") {
        bot.channels.get("409457023470534666").createInvite({maxAge: 0, maxUses: 25})
        .then(invite => message.channel.send(`25 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 4 30") {
        bot.channels.get("409457023470534666").createInvite({maxAge: 0, maxUses: 30})
        .then(invite => message.channel.send(`30 invitations à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
    if(message.content === "!invite 5 1") {
        bot.channels.get("409457205226504225").createInvite({maxAge: 0, maxUses: 1})
        .then(invite => message.channel.send(`1 invitation à ce channel : http://discord.gg/${invite.code}`))
        .catch(console.error);
    }
}